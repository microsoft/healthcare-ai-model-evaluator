# Vite Environment Variables
# Rename to .env.local for local development

# Base path for the application (defaults to '/webapp' if not set)
# Set to empty string '' or '/' for root deployment
VITE_BASE_PATH=/webapp

VITE_CLIENT_ID=
VITE_TENANT_ID=
VITE_API_BASE_URL=http://localhost:5000